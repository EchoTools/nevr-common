version: v2

modules:
  - path: .
    name: buf.build/echotools/nevr-api
deps:
  - buf.build/googleapis/googleapis
  - buf.build/grpc-ecosystem/grpc-gateway


lint:
  use:
    - STANDARD
  ignore_only:
    PACKAGE_DIRECTORY_MATCH:
    - .
breaking:
  use:
    - PACKAGE

